import React, { memo } from 'react';
import './CodeTyping.scss';

const CodeTyping: React.FC = () => {
  return (<div className="typing-wrapper"><div className="frse-portfolio">
  </div></div>);
}

export default memo(CodeTyping);